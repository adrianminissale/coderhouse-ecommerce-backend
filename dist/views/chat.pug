h1 Vista de Mensajes
hr
form(id='chat')
  .form-group
    input.form-control(type='email' name='email' placeholder='Ingrese su email' required='true')
    br

    div(id='chats')
      if chatsApi.length
        each chat in chatsApi
          div
            span.mail=chat.email
            span.time [#{chat.time}] :
            span.message=chat.message

  .form-inline
    input.form-control.mr-3.w-50(type='text' name='message' placeholder='Ingrese su mensaje' required='true')
    button.btn.btn-success(type='submit') Enviar